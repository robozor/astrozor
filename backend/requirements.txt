# Astrozor backend dependencies — pinned for reproducibility
# Regenerate via: docker compose run --rm api pip-compile requirements.in (later)

# Core
Django==5.1.4
django-ninja==1.3.0

# ASGI / WebSocket
channels==4.2.0
channels-redis==4.2.1
daphne==4.1.2
uvicorn[standard]==0.34.0
gunicorn==23.0.0

# Database
psycopg[binary]==3.2.3

# Background jobs
celery==5.4.0
redis==5.2.1

# Validation / schemas
pydantic[email]==2.10.4
email-validator==2.2.0

# HTTP
httpx==0.28.1

# Misc
python-dotenv==1.0.1
bleach==6.2.0
markdown-it-py==3.0.0
linkify-it-py==2.0.3
feedparser==6.0.11

# Dev / test
pytest==8.3.4
pytest-django==4.9.0
pytest-asyncio==0.25.2
ruff==0.8.6
